networks:
  radares-net:
    external: true

services:

  # ═══════════════════════════════════════════════
  #  MICROSSERVIÇO — Radar SPVias (Quarkus)
  # ═══════════════════════════════════════════════
  quarkus-radar-spvias:
    container_name: quarkus-radares-spvias
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8084:8084"
    env_file:
      - .env
    environment:
      - QUARKUS_PROFILE=prod

      # ── MongoDB ───────────────────────────────
      - MONGODB_URI=mongodb:${URI_MONGODB}
      - MONGODB_DATABASE=${DATABASE_MONGODB}

      # ── RabbitMQ ──────────────────────────────
      - RABBITMQ_HOST=rabbitmq
      - RABBITMQ_PORT=5672
      - RABBITMQ_USERNAME=${RABBITMQ_USER}
      - RABBITMQ_PASSWORD=${RABBITMQ_PASSWORD}
      - rabbitmq.exchange.name=radares_exchange
      - rabbitmq.routing.key=radares.spvias

      # ── CORS ──────────────────────────────────
      - CORS_ORIGINS=http://localhost:5173,http://localhost:3000
    networks:
      - radares-net
    restart: unless-stopped

    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider",
             "http://localhost:8088/actuator/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s